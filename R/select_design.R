# -design_select (grabs the design row with geometry, distance, and n)

select_design <- function(geometry, distance, n){
  #(grabs the design row with geometry, distance, and n)
  # maybe use subset function??
  # returns the row of DoEAna needed
}
